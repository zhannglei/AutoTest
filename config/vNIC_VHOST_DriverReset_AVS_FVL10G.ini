#!/usr/bin/env bash

# VM config
hosts=1
vms=1
#virtio pci-sriov avp
vif_model=avp
flavor=4cpuS0

# case
case_shell=./AutoTest/case/vNIC_AVP_DriverReset_AVS_FVL10G.sh

# test data

# add common config
common_configs="config/common_config.ini ../config/common_config"
for common_config in config/common_config.ini ../config/common_config;do
    if [ -f ${common_config} ];then
        . ${common_config}
        break
    fi
done